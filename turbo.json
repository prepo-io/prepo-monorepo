{
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "react-boilerplate#build": {
      "dependsOn": [
        "^build",
        "$SENTRY_ORG",
        "$SENTRY_PROJECT",
        "$NEXT_PUBLIC_SENTRY_DSN",
        "$SENTRY_AUTH_TOKEN",
        "$NPM_RC"
      ],
      "outputs": [".next/**", "out/**"]
    },
    "docs#build": {
      "dependsOn": ["^build", "$NPM_RC"],
      "outputs": ["build/**"]
    },
    "website#build": {
      "dependsOn": ["^build", "$NEXT_PUBLIC_RECAPTCHA_SITE_ID", "$NPM_RC"],
      "outputs": [".next/**", "out/**"]
    },
    "simulator#build": {
      "dependsOn": ["^build", "$NPM_RC"],
      "outputs": ["dist/**"]
    },
    "acquisition-royale-home#build": {
      "dependsOn": [
        "^build",
        "$NEXT_PUBLIC_NETWORK",
        "$SENTRY_ORG",
        "$SENTRY_PROJECT",
        "$NEXT_PUBLIC_SENTRY_DSN",
        "$SENTRY_AUTH_TOKEN",
        "$NPM_RC"
      ],
      "outputs": [".next/**", "out/**"]
    },
    "acquisition-royale-landing-page#build": {
      "dependsOn": ["^build", "$NEXT_PUBLIC_NETWORK", "$NPM_RC"],
      "outputs": [".next/**", "out/**"]
    },
    "test": {
      "outputs": [],
      "inputs": ["src/**/*.tsx", "src/**/*.ts", "test/**/*.ts"]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false
    },
    "compile": {
      "cache": false
    }
  }
}
