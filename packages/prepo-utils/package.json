{
  "name": "prepo-utils",
  "version": "0.0.0",
  "description": "prePO utils",
  "source": "src/index.ts",
  "main": "src/index.ts",
  "files": ["src/index.ts", "src/types.ts"],
  "scripts": {
    "build": "tsc",
    "test": "npx jest",
    "test:ci": "yarn test",
    "build:watch": "yarn build --watch"
  },
  "dependencies": {
    "prepo-constants": "*",
    "ethers": "5.6.8",
    "bnc-onboard": "1.38.3"
  },
  "devDependencies": {
    "config": "*",
    "tsconfig": "*"
  }
}
