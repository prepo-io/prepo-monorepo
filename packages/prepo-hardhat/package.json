{
  "name": "prepo-hardhat",
  "version": "0.0.0",
  "description": "prePO Hardhat config",
  "main": "src/index.ts",
  "files": ["src/index.ts", "src/types.ts"],
  "scripts": {
    "build": "tsc",
    "build:watch": "yarn build --watch"
  },
  "dependencies": {
    "prepo-constants": "*"
  },
  "devDependencies": {
    "config": "*",
    "tsconfig": "*"
  }
}
